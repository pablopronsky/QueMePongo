class Outfit {
  String type; // Tipo de ropa, por ejemplo, "pantalones cortos", "camiseta", etc.
  String color; // Color de la ropa.
  int temperature; // Temperatura para la que es adecuada la ropa.

  Outfit({
    required this.type,
    required this.color,
    required this.temperature,
  });
}
